<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ChatActivity"
    >
    <LinearLayout
        android:id="@+id/chat_heading_linear_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/holo_blue_light"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:textStyle="bold"
            android:textAlignment="center"
            android:layout_gravity="center_vertical"
            android:text="GUPSHUP CHAT"
            />

    </LinearLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/message_list_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/chat_heading_linear_layout"
        >

    </android.support.v7.widget.RecyclerView>

    <RelativeLayout
        android:id="@+id/writeLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="bottom"
        android:background="@android:color/white"
        android:elevation="2dp"
        android:gravity="bottom"
        android:padding="8dp">

        <RelativeLayout
            android:id="@+id/message_text_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toLeftOf="@id/sendButtonImageView"
            android:layout_toStartOf="@id/sendButtonImageView">

            <EditText
                android:id="@+id/messageText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginEnd="4dp"
                android:layout_marginRight="4dp"
                android:background="@android:color/white"
                android:elevation="2dp"
                android:hint="Type a message.."
                android:maxLines="1"
                android:padding="12dp"
                android:singleLine="true"
                android:enabled="false"
                />

        </RelativeLayout>

        <ImageView
            android:id="@+id/sendButtonImageView"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignBottom="@+id/message_text_layout"
            android:layout_alignParentRight="true"
            android:layout_alignTop="@+id/message_text_layout"
            android:src="@android:drawable/ic_menu_send" />

    </RelativeLayout>


</RelativeLayout>